export enum BroadcastChannel {
  IN_APP = 'in_app',
  PUSH = 'push',
  EMAIL = 'email',
}

export enum BroadcastTarget {
  ALL = 'all',
  PREMIUM = 'premium',
  UNVERIFIED = 'unverified',
  BLOCKED = 'blocked',
  ACTIVE = 'active',
}
