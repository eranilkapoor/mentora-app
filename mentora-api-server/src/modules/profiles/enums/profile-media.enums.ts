export enum MediaStatus {
  ACTIVE = 'active',
  DELETED = 'deleted',
  PROCESSING = 'processing',
}

export enum MediaModerationStatus {
  APPROVED = 'approved',
  FLAGGED = 'flagged',
  REJECTED = 'rejected',
  PENDING = 'pending',
}
