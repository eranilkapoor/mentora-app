export enum MediaStatus {
  ACTIVE = 'active',
  PROCESSING = 'processing',
  DELETED = 'deleted',
}

export enum MediaModerationStatus {
  PENDING = 'pending',
  APPROVED = 'approved',
  FLAGGED = 'flagged',
  REJECTED = 'rejected',
}
